# Hardware

The prototype is built around an ESP8266 NodeMCU and a set of sensors/actuators for environmental monitoring, motion detection, location tracking, access control and cooling.

See `components.md` for the component list and GPIO allocation. Images of the project and selected components are stored under `images/`.
